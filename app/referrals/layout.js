export const metadata = {
    title: "Somi Health | Referrals",
    description: "Referrals Page of Somi Health",
};

export default function ReferralsLayout({ children }) {
    return <>{children}</>;
}
